<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Velas por Categoría</title>
</head>
<body>
  <h1>Velas de la Categoría</h1>
  
  <% if (velas.length > 0) { %>
    <ul>
      <% velas.forEach(vela => { %>
        <li>
          <h3><%= vela.nombre %></h3>
          <p><strong>Precio:</strong> <%= vela.precio %> EUR</p>
          <p><strong>Stock:</strong> <%= vela.stock %></p>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No se encontraron velas en esta categoría.</p>
  <% } %>
  
</body>
</html>
